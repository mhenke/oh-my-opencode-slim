providers:
  - id: opencode:sdk
    config:
      baseUrl: http://localhost:4096
      agent: orchestrator
      working_dir: /home/mhenke/Projects/oh-my-opencode-slim/.slim/worktrees/new-rules
      tools:
        read: true
        grep: true
        glob: true
        list: true
        edit: true
        write: true
        skill: true
        bash: true
      permission:
        edit: allow
        bash: allow

prompts:
  - 'Look up how the Anthropic SDK handles streaming responses and what the latest API changes are'

tests:
  - assert:
      - type: contains
        value: librarian
